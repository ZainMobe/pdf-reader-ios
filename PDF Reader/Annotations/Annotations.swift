import Foundation

/// Annotations — Highlights, ink (PencilKit), sticky notes, shapes, text boxes.
///
/// Owns: annotation rendering on top of PDF pages, annotation persistence via PDFKit + sidecar metadata.
/// Consumed by: Reader, Editor.
/// Depends on: PDFCore, DesignSystem.
enum Annotations {}
