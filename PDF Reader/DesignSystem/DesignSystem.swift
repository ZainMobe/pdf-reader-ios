import Foundation

/// DesignSystem — Liquid Glass tokens, shared SwiftUI components, and adaptive layout helpers.
///
/// Owns: color palette, typography, spacing, motion, reusable views (cards, chips, badges).
/// Consumed by: every UI module.
/// Does NOT depend on: feature modules (Reader, Editor, Signing, etc.).
enum DesignSystem {}
